name: Deploy Site

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: 🚀 Deploy website every commit
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: ./
        with:
          server: ftp.thrivewellhealthcare.com
          username: thrivewell@thrivewellhealthcare.com
          password: ${{ secrets.ftp_password }}
          protocol: ftps
